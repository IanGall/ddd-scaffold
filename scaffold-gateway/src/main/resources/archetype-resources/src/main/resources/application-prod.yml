#set( $symbol_dollar = '$' )
dubbo:
  consumer:
    parameters:
      ssl-enabled: false
  registry:
    address: ${symbol_dollar}{DUBBO_REGISTRY_ADDRESS}
    username: ${symbol_dollar}{DUBBO_REGISTRY_USERNAME}
    password: ${symbol_dollar}{DUBBO_REGISTRY_PASSWORD}
    use-as-config-center: false
    use-as-metadata-center: true
