# 本地开发环境变量模板
#
# 使用：cp .env.example .env.local，填写真实值（.env.local 已被 .gitignore 忽略，不得入库）。
# 生产环境由部署平台注入同名环境变量，不依赖本文件。

MYSQL_USERNAME=root
MYSQL_PASSWORD=

REDIS_PASSWORD=

NACOS_USERNAME=
NACOS_PASSWORD=

# 渠道 AES 主密钥：Base64 编码的 32 字节随机值（openssl rand -base64 32）
CHANNEL_ENCRYPTION_MASTER_KEY=
