#set( $symbol_pound = '#' )
#set( $symbol_dollar = '$' )
#set( $symbol_escape = '\' )
/**
 * RPC Provider 实现包。
 *
 * 1. 该包存放 Dubbo Triple 服务提供者实现（{@code @DubboService}），消费方为网关等上游。
 * 2. 与 application-dev.yml 中 dubbo.scan.base-packages 指向的包保持一致。
 */
package ${package}.trigger.rpc;
