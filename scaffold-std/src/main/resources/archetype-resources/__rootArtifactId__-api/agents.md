# API 模板模块协作说明

## 模块定位
- 本目录是生成项目中的 API 模板层。
- 定义接口契约、请求响应对象与对外语义边界。

## 变更边界
- 允许修改：接口定义、DTO/VO 模板、参数校验模板。
- 禁止修改：应用编排、领域规则、基础设施实现。

## 依赖约束
- 仅依赖共享类型与最小公共能力。
- 不在 API 层模板引入持久化或外部适配实现。

## 提交前检查
- 字段命名语义清晰且兼容已有调用方。
- 清理未使用的模板字段与注释。
